### 3. **设置运行按钮**

如果你已经安装了 `Code Runner` 插件，但还是没有看到运行按钮，可能是因为没有正确配置运行环境。你可以通过以下方式手动添加运行按钮。

1. 打开 VS Code 设置（按下 `Ctrl + ,`）。
    
2. 搜索 `Code Runner Executor Map`，点击进入。
    
3. 在 `code-runner.executorMap` 配置中，确保 C++ 编译器使用的是正确的路径。通常应该像这样：